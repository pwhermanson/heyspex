/**
 * Settings feature state exports
 *
 * This module provides all state management for the settings feature,
 * including application settings and preferences.
 */

export { useSettingsStore } from './settings-store';

// Re-export types for convenience
export type { AppSettings } from './settings-store';
